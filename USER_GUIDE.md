# 🍷 NightWork POS システム - 完全ユーザーガイド

Version 3.0.0 - キャバクラ特化統合POSシステム

---

## 📋 目次

1. [システム概要](#システム概要)
2. [クイックスタートガイド](#クイックスタートガイド)
3. [役割別機能ガイド](#役割別機能ガイド)
4. [主要機能の詳細](#主要機能の詳細)
5. [技術仕様とアーキテクチャ](#技術仕様とアーキテクチャ)
6. [トラブルシューティング](#トラブルシューティング)
7. [よくある質問](#よくある質問)

---

## 🎯 システム概要

### 基本コンセプト

NightWork POSは、キャバクラ業界に特化した統合POSシステムです。以下の特徴を持ちます：

- テーブルファースト設計: 各テーブルに設置されたiPadから直接注文・指名・サービス要請が可能
- 4つのロール: テーブルログイン、キャスト、管理者、システム管理者
- リアルタイム管理: テーブル状況、注文、セッションをリアルタイムで監視
- 包括的な管理: 顧客管理、売上分析、給与計算、勤怠管理まで一元化

### 主要機能

1. テーブル管理: テーブル別のセッション管理、注文、指名、サービス注文
2. 指名システム: 本指名・場内指名・一緒指名の完全サポート
3. バック率計算: ドリンク・ボトル・指名別の自動計算
4. 給与計算: 勤怠・バック率・指名料を考慮した自動給与計算
5. 売上管理: 日次・月次売上レポート、キャスト別実績
6. 顧客管理: VIP管理、指名履歴、来店履歴
7. メニュー管理: カテゴリ・商品管理、画像アップロード対応
8. Bluetooth印刷: EPSONプリンターとの連携（Chrome/Edge対応）

---

## 🚀 クイックスタートガイド

### 初回ログイン

1. ブラウザでアプリケーションにアクセス
   - 推奨ブラウザ: Google Chrome、Microsoft Edge（Bluetooth印刷機能を使用する場合）
   - Safari、Firefoxでも基本機能は利用可能

2. ログインモーダルを開く
   - ホームページの「ログイン」ボタンをクリック

3. 役割を選択
   - テーブルログイン: テーブルでの注文・指名・サービス要請
   - キャスト: 勤怠管理・給与確認・指名管理
   - 管理者: 店舗運営・顧客管理・売上管理
   - システム管理者: 複数店舗管理・監査ログ

4. 認証情報を入力
   - 各ロールに応じたIDとパスワードを入力

5. ダッシュボードにアクセス
   - ログイン後、役割別のダッシュボードが表示されます

---

## 👥 役割別機能ガイド

### 🍽️ テーブルログイン（お客様向け）

#### アクセス方法
- URL: `/table/[tableId]`
- テーブルに設置されたiPadからアクセス

#### 主要機能

##### 1. 注文機能（注文タブ）

カテゴリ表示
- カテゴリタブが画面上部に固定表示（スクロール時も固定）
- 連続した平行四辺形形式で表示
- タップでカテゴリを切り替え

商品表示
- グリッド形式で商品を表示
  - モバイル: 2列
  - タブレット: 3列
  - デスクトップ: 4列
- 各商品カードに以下を表示:
  - 商品画像
  - 商品名
  - SKU
  - 価格（右下にオーバーレイ表示）
- 商品をタップすると注文モーダルが開く

注文モーダル
- 数量選択（+/-ボタン）
- キャスト選択（オプション）
- 「カートに追加」ボタンでカートに追加

注文カート
- 画面左下にピンクのグラデーション円形ボタン
- カート内の商品一覧を表示
- 注文リクエスト送信機能
- 注文リクエスト送信済みの場合、ボタンに赤いドットが表示

技術仕様
- カテゴリタブ: `z-index: 30`（商品より上、ヘッダー・下部ナビより下）
- 商品グリッド: レスポンシブ対応（Tailwind CSS）
- カート: Sheetコンポーネントでサイドスライド表示

##### 2. 指名機能（指定タブ）

指名タイプ選択
- 本指名: 常連のお客様が特定のキャストを指名
- 場内指名: 店内でキャストを指名
- 各ボタンに背景画像を設定可能（`public/assets/nomination/1.jpeg`, `2.jpeg`）

指名リスト
- 現在のセッションでの指名履歴を表示
- 指名タイプ、キャスト名、金額を表示

技術仕様
- 指名データは`nomination`テーブルに保存
- バック率計算に自動反映

##### 3. サービス機能（サービスタブ）

サービス・呼び出し
- おしぼり、灰皿交換、グラス交換、箸交換などのサービス要請
- スタッフ呼び出し（管理・サービス・緊急）

追加注文
- サービス商品の注文

サービス表示
- グリッド形式で表示（1行に3つ）
- モバイル: 2列
- タブレット以上: 3列

サービス注文カート
- 画面右側に表示
- サービス注文の一覧と送信機能

##### 4. セット延長機能

延長設定
- 延長時間の設定
- ゲスト数の入力
- 延長料金の自動計算

延長履歴
- 過去の延長記録を表示
- 延長時間、ゲスト数、料金を表示

##### 5. 注文合計表示

リアルタイム合計
- 現在のセッションの注文合計を表示
- 商品、指名、延長料金を含む

支払い機能
- 現金支払い
- クレジットカード支払い（Stripe連携）
- 店舗カード支払い

##### 6. セッション管理

セッション開始
- テーブルにアクセスすると自動的にセッションが開始
- セッション状態の表示（アクティブ・一時停止・終了）

セッション一時停止/再開
- 「一時停止」ボタンでセッションを一時停止
- 一時停止中は「停止中」テキストが時間表示領域の右上に表示
- 「再開」ボタンでセッションを再開

セッション終了
- 「退店」ボタンでセッションを終了
- 会計処理後にセッション終了

技術仕様
- セッション状態は`sessions`テーブルで管理
- 状態: `active`, `paused`, `completed`

##### 7. 下部ナビゲーションバー

固定ナビゲーション
- 画面下部に固定表示（`position: fixed`, `z-index: 50`）
- 4つのタブ:
  - 最初のページ: ホーム画面
  - 注文: 商品注文
  - 指定: キャスト指名
  - サービス: サービス要請

レスポンシブ対応
- モバイル: 全幅表示
- デスクトップ: 左側コンテンツエリア（5/6幅）に合わせて表示

---

### 🎭 キャスト機能

#### アクセス方法
- URL: `/cast/dashboard`
- キャストIDとパスワードでログイン

#### 主要ページ

##### 1. 勤怠管理 (`/cast/attendance`)

機能
- 出退勤記録
- 休憩時間管理（開始・終了）
- 勤怠履歴表示
- 稼働時間の自動計算

操作方法
1. 「出勤」ボタンをクリックして出勤記録
2. 「退勤」ボタンをクリックして退勤記録
3. 「休憩開始」/「休憩終了」で休憩時間を管理
4. 勤怠履歴テーブルで過去の記録を確認

技術仕様
- データは`attendance`テーブルに保存
- 状態: `active`, `paused`, `completed`
- 自動計算: 稼働時間 = 退勤時刻 - 出勤時刻 - 休憩時間

##### 2. 給与確認 (`/cast/payroll`)

機能
- 給与明細の表示
- 給与履歴の確認
- バック率の確認
- 指名料の確認

表示内容
- 基本給
- ドリンクバック
- ボトルバック
- 本指名料
- 場内指名料
- 合計給与

技術仕様
- 給与計算は`payroll-calculator.ts`で実装
- バック率は`cast-back-system.ts`で計算

##### 3. バック率確認 (`/cast/back-rates`)

機能
- 現在のバック率設定を表示
- バック率履歴の確認
- 計算例の表示

表示内容
- ドリンクバック率
- ボトルバック率
- 本指名バック率
- 場内指名バック率
- 一緒指名バック率

##### 4. 指名管理 (`/cast/nominations`)

機能
- 本指名顧客の管理
- 指名統計の表示
- 指名履歴の確認
- 場内指名から本指名への昇格管理

操作方法
1. 本指名顧客一覧で顧客を確認
2. 指名統計で実績を確認
3. 指名履歴で過去の指名を確認

##### 5. サービス管理 (`/cast/service`)

機能
- サービス注文の対応
- スタッフ呼び出しの対応
- 対応履歴の確認

操作方法
1. サービス注文一覧で未対応の注文を確認
2. 「対応」ボタンをクリックして対応
3. 対応履歴で過去の対応を確認

##### 6. セッション詳細 (`/cast/sessions/[id]`)

機能
- セッション情報の表示
- 注文履歴の確認
- 参加キャスト情報の確認
- 顧客情報の確認

---

### 👨‍💼 管理者機能

#### アクセス方法
- URL: `/admin/dashboard`
- 管理者IDとパスワードでログイン

#### 主要ページ

##### 1. 日次売上 (`/admin/sales/daily`)

機能
- 日別売上チャート表示
- 売上データの確認・分析
- テーブル別売上表示
- キャスト別売上表示
- 商品別売上表示
- 時間帯別売上表示
- 控除項目の管理
- CSV/Excelエクスポート機能

操作方法
1. 日付を選択（カレンダーまたは日付入力）
2. 売上チャートでトレンドを確認
3. テーブル別・キャスト別・商品別・時間帯別の売上データを確認
4. 控除項目を追加・編集・削除
5. 「エクスポート」ボタンでCSV/Excel形式でダウンロード

技術仕様
- データは`sessions`、`order`、`nomination`テーブルから集計
- リアルタイム更新対応
- 日次レポートは`/api/admin/sales/daily`から取得

##### 2. 月次売上 (`/admin/sales/monthly`)

機能
- 月別売上レポート
- 売上分析・統計
- カテゴリ別売上表示
- 商品別売上表示
- キャスト別売上表示
- 日別売上推移表示
- 日次レジ締めチェック

操作方法
1. 年・月を選択
2. 月次売上レポートを確認
3. カテゴリ別・商品別・キャスト別の売上統計を確認
4. 日別売上推移チャートでトレンドを確認
5. 日次レジ締めチェックテーブルで確認状況を確認

技術仕様
- データは`/api/admin/sales/monthly`から取得
- 日次レジ締めチェックは`/api/admin/sales/daily-check`から取得
- 月次集計は日次データを統合して計算

##### 3. メニュー管理 (`/admin/menu`)

カテゴリ管理 (`/admin/menu/categories`)

機能
- カテゴリ一覧の表示
- カテゴリ登録・編集・削除
- 画像アップロード

操作方法
1. 「新規登録」ボタンでカテゴリを登録
2. カテゴリ名、画像を入力
3. 「保存」ボタンで保存
4. カテゴリ一覧から編集・削除が可能

技術仕様
- 画像は自動圧縮（最大1280px、WebP形式、品質0.82）
- データは`category`テーブルに保存
- 画像はBase64形式でデータベースに保存

商品管理 (`/admin/menu/products`)

機能
- 商品一覧の表示・検索・フィルタリング
- 商品登録・編集・削除
- 画像アップロード
- SKU管理
- 在庫管理

操作方法
1. 「新規登録」ボタンで商品を登録
2. 商品名、SKU、価格、在庫数、画像を入力
3. カテゴリを選択
4. 「保存」ボタンで保存
5. 商品一覧から検索・フィルタリング・編集・削除が可能

技術仕様
- 画像は自動圧縮（最大1280px、WebP形式、品質0.82）
- データは`product`テーブルに保存
- SKUは一意制約あり

##### 4. 勤怠承認 (`/admin/attendance`)

機能
- 勤怠一覧の表示（リアルタイム更新）
- 勤怠承認・修正
- 勤怠詳細の確認
- 勤怠統計の表示
- 出退勤時刻の修正
- コメントの追加

操作方法
1. 勤怠一覧で未承認の勤怠を確認（承認待ちは黄色バッジで表示）
2. 「詳細」ボタンで勤怠詳細を確認
3. 「承認」ボタンで承認（承認済みは緑色バッジで表示）
4. 「修正」ボタンで勤怠情報を修正
5. 出退勤時刻、総勤務時間、コメントを編集可能

技術仕様
- データは`attendance`テーブルに保存
- 状態: `pending`（承認待ち）、`approved`（承認済み）、`rejected`（却下）、`saved`（保存済み）
- リアルタイム更新（500msごと）
- 詳細時間記録（`detailed_times`）から総勤務時間を自動計算

##### 5. 給与計算 (`/admin/payroll/preview`)

機能
- 給与計算・プレビュー
- 給与確定
- 給与明細生成
- Bluetooth印刷対応
- 期間別・キャスト別給与計算

操作方法
1. 対象期間を選択（開始日・終了日）
2. キャストを選択（全員または個別）
3. 「計算」ボタンで給与を計算
4. 給与明細をプレビューで確認
5. 「確定」ボタンで給与を確定
6. 「印刷」ボタンでBluetoothプリンターに印刷

技術仕様
- 給与計算は`payroll-calculator.ts`で実装
- 基本給、ドリンクバック、ボトルバック、指名料を自動計算
- 印刷はESC/POS形式でBluetooth経由
- 給与明細は`salary_full`テーブルに保存

##### 6. ボトル保管管理 (`/admin/bottle-keep`)

機能
- ボトル保管一覧の表示
- ボトル保管情報の検索
- ボトル保管情報の編集・削除
- 顧客別・テーブル別の管理

操作方法
1. ボトル保管一覧で全保管情報を確認
2. 検索バーで顧客名・メールアドレス・ボトル名で検索
3. 「編集」ボタンで保管情報を編集
4. 「削除」ボタンで保管情報を削除

技術仕様
- データは`bottle_keep`テーブルに保存
- 顧客名、メールアドレス、ボトル名、数量、セッションID、テーブルIDを管理

##### 7. シフト管理 (`/admin/shifts`)

機能
- シフト一覧の表示（カレンダー形式）
- シフト登録・削除
- 月別表示・前後月への移動
- キャスト別シフト管理

操作方法
1. カレンダーで月を選択（前月・次月ボタンで移動）
2. キャスト名と日付の交差するセルをクリック
3. シフト登録確認ダイアログで「登録」をクリック
4. 登録済みシフトはハートアイコンで表示
5. 登録済みシフトをクリックして削除

技術仕様
- データは`shift`テーブルに保存
- カレンダー形式で月別表示
- キャストIDと日付でシフトを管理

##### 8. 料金設定 (`/admin/add-charges`)

機能
- 各種料金の設定・管理
- 本指名料・場内指名料・同伴料の設定
- ボトル保管料・個室使用料・カラオケ利用料の設定
- セット料金・延長料金の設定

設定項目
- 本指名料: 来店時に発生する本指名料
- 場内指名料: 店内滞在に対して発生する指名料
- 同伴料: キャストとの同伴にかかる料金
- ボトル保管料: ボトルをキープする際の料金
- 個室使用料: VIPルーム利用時の追加料金
- カラオケ利用料: カラオケ設備の利用に対する料金
- セット料金: 基本セット料金
- 延長料金: セット延長時の料金

操作方法
1. 料金一覧で各料金項目を確認
2. 「編集」ボタンで料金を編集
3. 料金値と備考を入力
4. 「保存」ボタンで保存

技術仕様
- データは`add_charges`テーブルに保存
- 料金名（`charge_name`）と値（`value`）で管理

##### 9. 給与項目管理 (`/admin/salary-settings`)

機能
- 給与カテゴリ設定
- 基本給設定（時給・日給・月給）
- 指名バック率設定（統合機能）
- バック率設定（ドリンク・ボトル・フード）

設定項目
- 基本給設定: 時給・日給・月給の設定
- 指名バック率: 本指名・場内指名・一緒指名のバック率設定
- バック率設定: ドリンク・ボトル・フードのバック率設定

操作方法
1. 左側の設定分類から項目を選択
2. 右側に設定フォームが表示
3. 値を入力して「保存」ボタンで保存

技術仕様
- 設定は`salary_category`テーブルに保存
- 指名バック率は`NominationBackRatesPanel`コンポーネントで表示

##### 10. スタッフ管理 (`/admin/casts`)

機能
- スタッフ（キャスト）一覧の表示
- スタッフ登録・編集・削除
- 管理者登録表の管理（スーパー管理者のみ）
- スタッフと管理者のタブ切り替え

操作方法
1. 「スタッフ」タブまたは「管理者」タブを選択
2. 「スタッフ追加」または「管理者登録」ボタンで新規登録
3. スタッフ名・メールアドレス・パスワード・備考を入力
4. 一覧から「編集」ボタンで情報を変更
5. 「削除」ボタンで削除

権限制御
- 管理者登録表へのアクセス権限は、`user`テーブルで`role`が`super_admin`であるスーパー管理者のみ
- スーパー管理者以外は「管理者」タブが表示されない

技術仕様
- スタッフデータは`user`テーブルに`role='cast'`で保存
- 管理者データは`user`テーブルに`role='admin'`で保存
- パスワードはハッシュ化して保存

##### 11. スタッフ呼び出し (`/admin/staff-calls`)

機能
- 呼び出し一覧の表示
- 呼び出し対応
- 対応履歴の確認
- 統計表示
- 呼び出しタイプ別管理（管理・サービス・緊急）

操作方法
1. 呼び出し一覧で未対応の呼び出しを確認
2. 呼び出しタイプ（管理・サービス・緊急）でフィルタリング
3. 「対応」ボタンで対応
4. 対応履歴で過去の対応を確認
5. 統計で呼び出し実績を確認

技術仕様
- データは`staff_call`テーブルに保存
- 呼び出しタイプ: `management`（管理）、`service`（サービス）、`emergency`（緊急）

##### 12. 注文監視 (`/admin/order-monitoring`)

機能
- 注文一覧の表示（リアルタイム更新）
- 注文状況の確認
- 注文承認・却下
- サービス注文の管理
- 処理履歴の確認

操作方法
1. 注文一覧で全注文を確認（売上注文とサービス注文のタブ切り替え）
2. 注文状況を確認（pending: 承認待ち、sent: 送信済み、accepted: 承認済み、rejected: 却下）
3. 「承認」ボタンで注文を承認
4. 「却下」ボタンで注文を却下

技術仕様
- 売上注文は`salesorder`テーブルから取得
- サービス注文は`serviceorder`テーブルから取得
- リアルタイム更新（500msごと）

##### 13. テーブル管理 (`/admin/tables`)

機能
- テーブル一覧の表示
- テーブル登録・編集・削除
- テーブル状態の管理（空席・利用中・予約済・清掃中）
- セッション管理
- リアルタイムテーブル状態の確認

操作方法
1. テーブル一覧で全テーブルを確認
2. 「新規登録」ボタンでテーブルを登録
3. テーブル名・収容人数・備考を入力
4. テーブルをクリックして詳細を確認
5. 状態を手動で変更可能
6. 右下の「テーブル状態」ボタンでリアルタイム状態を確認

技術仕様
- テーブルデータは`table`テーブルに保存
- リアルタイム更新（30秒ごと）
- リアルタイムテーブル状態ページでは、稼働中のテーブルにアクセス可能
- 注文合計部分の合計に対する変更権限は、スーパー管理者のみ

##### 14. サービス管理 (`/admin/services`)

機能
- サービス一覧の表示
- サービス登録・編集・削除
- サービス名・備考の管理

操作方法
1. サービス一覧で全サービスを確認
2. 「新規登録」ボタンでサービスを登録
3. サービス名・備考を入力
4. 「保存」ボタンで保存
5. 一覧から「編集」ボタンで情報を変更
6. 「削除」ボタンで削除

技術仕様
- データは`service`テーブルに保存
- サービス名と備考（`other`）で管理

##### 15. 管理者情報管理 (`/admin/profile`)

機能
- 管理者情報の表示
- 管理者名・メールアドレスの変更
- パスワードの変更

操作方法
1. 管理者情報ページで現在の情報を確認
2. 管理者名・メールアドレスを編集して「保存」ボタンで保存
3. 新しいパスワードを入力して「パスワード変更」ボタンで変更

技術仕様
- データは`user`テーブルに保存
- パスワードはハッシュ化して保存
- 管理者認証情報（`admin_auth`）は`localStorage`に保存

---

### 🔧 システム管理者機能

#### アクセス方法
- URL: `/super/stores`
- システム管理者IDとパスワードでログイン

#### 主要ページ

##### 1. 店舗管理 (`/super/stores`)

機能
- 店舗一覧の表示
- 店舗登録・編集
- 店舗設定管理
- 設定テンプレート配布

操作方法
1. 「新規登録」ボタンで店舗を登録
2. 店舗名、住所、設定を入力
3. 「保存」ボタンで保存

##### 2. 監査ログ (`/super/audit`)

機能
- システム操作ログの表示
- 日付範囲フィルタリング
- ログ検索
- ログ分析
- ログエクスポート

操作方法
1. 日付範囲を選択
2. ログを検索
3. ログを確認
4. 「エクスポート」ボタンでエクスポート

---

## 🔧 主要機能の詳細

### Bluetooth印刷機能

#### 対応ブラウザ
- Google Chrome: 完全対応（Web Bluetooth API使用）
- Microsoft Edge: 完全対応（Web Bluetooth API使用）
- Safari: 基本印刷機能のみ（`window.print()`使用）
- Firefox: 基本印刷機能のみ（`window.print()`使用）

#### 使用方法

1. プリンター接続（Chrome/Edge）

1. 管理者ダッシュボードのヘッダーにある「プリンター」ボタンをクリック
2. 「周辺のBluetoothデバイスを検索」ボタンをクリック
3. デバイス選択ダイアログでEPSONプリンターを選択
4. 接続が完了すると「接続済み」と表示

2. 印刷実行

1. 印刷したいページ（例: 給与プレビュー）を開く
2. 「印刷」ボタンをクリック
3. ESC/POS形式でBluetooth経由で印刷

3. Safari/Firefoxでの印刷

1. 「プリンター」ボタンをクリック
2. 「OSの印刷ダイアログを開く（テスト印刷）」ボタンをクリック
3. OSの印刷ダイアログで印刷設定を行い、印刷

技術仕様
- Web Bluetooth APIを使用（Chrome/Edge）
- ESC/POS形式で印刷データを生成
- 接続状態は`PrinterContext`で管理
- 最後に接続したデバイスIDは`localStorage`に保存（自動再接続）

### 画像アップロード機能

#### 対応機能
- カテゴリ画像アップロード
- 商品画像アップロード

#### 使用方法

1. 画像選択
1. カテゴリ/商品登録・編集画面で「画像を選択」ボタンをクリック
2. ファイル選択ダイアログで画像を選択
3. 画像が自動的にプレビュー表示

2. 画像制限
- 最大ファイルサイズ: 5MB
- 対応形式: JPEG, PNG, WebP
- 自動圧縮: 最大1280px（長辺）、WebP形式、品質0.82

3. 保存
1. 画像を選択後、「保存」ボタンをクリック
2. 画像が自動的に圧縮され、Base64形式でデータベースに保存

技術仕様
- クライアント側で画像を圧縮（`lib/image/compress.ts`）
- Canvas APIを使用してリサイズ
- WebP形式で保存（透明度がある場合はPNG）
- Base64形式でデータベースに保存

### レスポンシブデザイン

#### 対応デバイス
- モバイル: 320px - 480px（スマートフォン）
- タブレット: 768px - 1024px（iPad等）
- デスクトップ: 1024px以上（PC）

#### 主要なレスポンシブ機能

1. グリッドレイアウト
- モバイル: 1列
- タブレット: 2-3列
- デスクトップ: 3-4列

2. フォントサイズ
- モバイル: `text-sm`, `text-base`
- タブレット: `text-base`, `text-lg`
- デスクトップ: `text-lg`, `text-xl`

3. パディング・マージン
- モバイル: `p-4`, `m-4`
- タブレット: `p-6`, `m-6`
- デスクトップ: `p-8`, `m-8`

4. ナビゲーション
- モバイル: 縦並び、アイコン中心
- タブレット: 横並び、アイコン+テキスト
- デスクトップ: 横並び、フルテキスト

技術仕様
- Tailwind CSSのブレークポイントを使用
- `sm:`, `md:`, `lg:`, `xl:`プレフィックスでレスポンシブ対応
- モバイルファーストアプローチ

### セッション管理機能

#### セッション状態
- active: アクティブ（利用中）
- paused: 一時停止
- completed: 完了

#### セッション操作

1. セッション開始
- テーブルにアクセスすると自動的にセッションが開始
- セッションIDが生成され、`sessions`テーブルに保存

2. セッション一時停止
- 「一時停止」ボタンをクリック
- セッション状態が`paused`に変更
- 「停止中」テキストが時間表示領域の右上に表示

3. セッション再開
- 「再開」ボタンをクリック
- セッション状態が`active`に戻る

4. セッション終了
- 「退店」ボタンをクリック
- 会計処理後にセッション状態が`completed`に変更

技術仕様
- セッションデータは`sessions`テーブルに保存
- セッション状態は`session-context.tsx`で管理
- リアルタイム更新（Socket.IO使用）

### バック率計算機能

#### 計算対象
- ドリンクバック: ドリンク注文に対するバック率
- ボトルバック: ボトル注文に対するバック率
- 本指名バック: 本指名に対するバック率
- 場内指名バック: 場内指名に対するバック率
- 一緒指名バック: 一緒指名に対するバック率

#### 計算方法

1. ドリンクバック計算
```
バック額 = ドリンク注文額 × ドリンクバック率 / 100
```

2. ボトルバック計算
```
バック額 = ボトル注文額 × ボトルバック率 / 100
```

3. 指名バック計算
```
バック額 = 指名料 × 指名バック率 / 100
```

技術仕様
- バック率計算は`cast-back-system.ts`で実装
- バック率は`user`テーブルの各カラムに保存
- 給与計算時に自動的に適用

### 給与計算機能

#### 計算項目
- 基本給: 時給 × 稼働時間
- ドリンクバック: ドリンク注文に対するバック
- ボトルバック: ボトル注文に対するバック
- 本指名料: 本指名に対するバック
- 場内指名料: 場内指名に対するバック
- 一緒指名料: 一緒指名に対するバック

#### 計算式
```
合計給与 = 基本給 + ドリンクバック + ボトルバック + 本指名料 + 場内指名料 + 一緒指名料
```

技術仕様
- 給与計算は`payroll-calculator.ts`で実装
- 勤怠データ、注文データ、指名データから自動計算
- 給与明細は`salary_full`テーブルに保存

---

## 🛠️ 技術仕様とアーキテクチャ

### フロントエンド

#### フレームワーク・ライブラリ
- Next.js: 14.0.0（App Router）
- React: 18.2.0
- TypeScript: 5.2.2
- Tailwind CSS: 3.3.3
- shadcn/ui: UIコンポーネントライブラリ
- Radix UI: アクセシブルなUIプリミティブ
- React Hook Form: フォーム管理
- Zod: バリデーション
- Recharts: データ可視化
- Socket.IO Client: リアルタイム通信

#### 状態管理
- React Context API: グローバル状態管理
  - `SessionContext`: セッション管理
  - `NotificationContext`: 通知管理
  - `PrinterContext`: プリンター管理

#### ルーティング
- Next.js App Router: ファイルベースルーティング
- 動的ルート: `[tableId]`, `[id]`など

### バックエンド

#### API
- Next.js API Routes: `/app/api/*`
- PostgreSQL: データベース
- pg: PostgreSQLクライアント

#### データベーススキーマ

主要テーブル
- `user`: ユーザー情報（キャスト、管理者）
- `table`: テーブル情報
- `sessions`: セッション情報
- `order`: 注文情報
- `nomination`: 指名情報
- `attendance`: 勤怠情報
- `salary_full`: 給与情報
- `category`: カテゴリ情報
- `product`: 商品情報
- `customer`: 顧客情報

技術仕様
- PostgreSQL 14+
- トランザクション対応
- 外部キー制約
- インデックス最適化

### セキュリティ

#### 認証・認可
- ロールベースアクセス制御（RBAC）
  - `table`: テーブルログイン
  - `cast`: キャスト
  - `admin`: 管理者
  - `super_admin`: システム管理者

#### データ保護
- パスワードハッシュ化: bcryptjs
- SQLインジェクション対策: パラメータ化クエリ
- XSS対策: Reactの自動エスケープ

### パフォーマンス

#### 最適化
- 画像圧縮: クライアント側で自動圧縮
- コード分割: Next.jsの自動コード分割
- キャッシング: ブラウザキャッシュ、Next.jsキャッシュ
- インデックス: データベースインデックス最適化

---

## 🔧 トラブルシューティング

### よくある問題と解決方法

#### 1. ログインできない

症状: ログインボタンをクリックしてもログインできない

原因
- 認証情報の誤り
- ネットワーク接続の問題
- ブラウザキャッシュの問題

解決方法
1. 認証情報を確認
2. ネットワーク接続を確認
3. ブラウザキャッシュをクリア
4. ブラウザを再起動

#### 2. Bluetoothプリンターに接続できない

症状: プリンター接続ボタンをクリックしても接続できない

原因
- Web Bluetooth APIがサポートされていないブラウザ
- プリンターがBluetoothでペアリングされていない
- プリンターの電源が入っていない

解決方法
1. ChromeまたはEdgeブラウザを使用
2. プリンターのBluetooth設定を確認
3. プリンターの電源を確認
4. デバイスのBluetooth設定でプリンターをペアリング

#### 3. 画像がアップロードできない

症状: 画像を選択してもアップロードできない

原因
- ファイルサイズが5MBを超えている
- 対応していない画像形式
- ネットワーク接続の問題

解決方法
1. 画像サイズを5MB以下に圧縮
2. JPEG、PNG、WebP形式を使用
3. ネットワーク接続を確認

#### 4. セッションが開始できない

症状: テーブルにアクセスしてもセッションが開始されない

原因
- テーブルが既に利用中
- データベース接続の問題
- セッション管理のエラー

解決方法
1. テーブル状態を確認
2. データベース接続を確認
3. ページを再読み込み
4. 管理者に連絡

#### 5. 給与計算が正しくない

症状: 給与計算結果が期待と異なる

原因
- バック率設定の誤り
- 勤怠データの不整合
- 計算ロジックのエラー

解決方法
1. バック率設定を確認
2. 勤怠データを確認
3. 注文データを確認
4. 管理者に連絡

---

## ❓ よくある質問

### Q1: テーブルサービスページで注文できない場合は？

A: 以下の点を確認してください：
1. セッションが開始されているか確認
2. 商品が選択されているか確認
3. ネットワーク接続を確認
4. ページを再読み込み

### Q2: キャストのバック率を変更するには？

A: 管理者画面の「バック率管理」または「給与設定」から変更できます。

### Q3: 給与計算はいつ実行されますか？

A: 管理者が「給与プレビュー」ページで手動で計算・確定します。自動計算はありません。

### Q4: 画像を削除するには？

A: カテゴリ/商品編集画面で画像を削除し、「保存」ボタンをクリックしてください。

### Q5: セッションを手動で終了するには？

A: テーブルサービスページの「退店」ボタンをクリックしてください。管理者画面からも終了可能です。

### Q6: 複数のキャストが同じテーブルを担当できますか？

A: はい、1つのセッションに複数のキャストが参加できます。

### Q7: 注文履歴はどこで確認できますか？

A: 
- キャスト: セッション詳細画面
- 管理者: 注文監視ページ、テーブル管理ページ

### Q8: システムのバックアップは取られていますか？

A: 定期的にデータのバックアップが取られています。詳細はシステム管理者にお問い合わせください。

### Q9: モバイルデバイスでも使用できますか？

A: はい、レスポンシブデザインにより、スマートフォン、タブレット、PCすべてで使用できます。

### Q10: プリンターはどの機種に対応していますか？

A: ESC/POSプロトコルに対応したBluetoothプリンター（EPSON推奨）に対応しています。

---

## 📞 サポート・連絡先

### 技術サポート
- システム管理者: 内線 100
- 緊急時: 24時間対応ホットライン
- メール: support@nightwork-pos.com

### 操作サポート
- 店舗管理者: 各店舗の管理者
- トレーニング: 定期的な操作研修を実施

---

## 📝 更新履歴

### Version 3.0.0 (2024年12月)
- テーブルサービスページのリデザイン
- 画像アップロード機能の追加
- Bluetooth印刷機能の追加
- レスポンシブデザインの改善
- 給与設定ページへの指名バック率統合

---

最終更新: 2024年12月  
バージョン: 3.0.0  
作成者: NightWork POS開発チーム
